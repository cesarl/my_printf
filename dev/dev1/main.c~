/* ------------------------ main  ------------------------ */

#include "my.h"

int	main (int argn, char **args)
{
  int	cnt;

  cnt = 0;
  while (args[cnt])
    {
      my_putstr(args[cnt]);
      cnt += 1;
    }
  return (0);
}
